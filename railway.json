{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c 'python manage.py migrate --noinput && gunicorn service_bay.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --threads 2 --log-level debug --access-logfile - --error-logfile -'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
